# Copy to .env and fill in. Never commit .env.

# --- OpenAI: one key covers STT (Whisper), chat (tool calling), and TTS ---
OPENAI_API_KEY=sk-your-key-here
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_STT_MODEL=whisper-1
OPENAI_TTS_MODEL=tts-1
OPENAI_TTS_VOICE=alloy

# --- Restaurant search provider: "foursquare" (primary) or "geoapify" (fallback) ---
PLACES_PROVIDER=foursquare
FOURSQUARE_API_KEY=
GEOAPIFY_API_KEY=

# --- App ---
DATABASE_URL=sqlite:///./data/sarjy.db
APP_ENV=dev
LOG_LEVEL=INFO

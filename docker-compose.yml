services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DATABASE_URL: sqlite:////data/sarjy.db
    volumes:
      - sarjy-data:/data

volumes:
  sarjy-data:
